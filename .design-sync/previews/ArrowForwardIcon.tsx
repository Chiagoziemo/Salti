import { ArrowForwardIcon } from '@salti/design-system';

export function Default() {
  return (
    <div style={{ color: '#3a5448', padding: 24 }}>
      <ArrowForwardIcon size={40} />
    </div>
  );
}
