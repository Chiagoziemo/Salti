import { ShoppingBasketIcon } from '@salti/design-system';

export function Default() {
  return (
    <div style={{ color: '#3a5448', padding: 24 }}>
      <ShoppingBasketIcon size={40} />
    </div>
  );
}
